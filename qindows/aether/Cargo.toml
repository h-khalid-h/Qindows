[package]
name = "aether"
version.workspace = true
edition.workspace = true
description = "Aether — Vector Compositor Engine for Qindows"

[dependencies]
libm = { workspace = true }
# No kernel dependencies — Aether runs in user space
qindows-types = { workspace = true }
