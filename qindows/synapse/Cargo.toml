[package]
name = "synapse"
version.workspace = true
edition.workspace = true
description = "Q-Synapse — Neural Integration (BCI)"

[dependencies]
libm = { workspace = true }
qindows-types = { workspace = true }
